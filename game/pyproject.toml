[project]
name = "delta-wrecker-client"
version = "0.1.0"
description = "Python Pygame client for Delta Wrecker"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pygame>=2.6.1",
]
